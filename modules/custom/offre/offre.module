<?php

function offre_theme_suggestions_offre(array $variables) {
  $suggestions = array();

  $sanitized_view_mode = strtr($variables['elements']['#view_mode'], '.', '_');

  $suggestions[] = 'offre__' . $sanitized_view_mode;
  $suggestions[] = 'offre__' . $annonce->id();

  return $suggestions;
}